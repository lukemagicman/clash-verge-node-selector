# 中间节点选择器 使用说明

一个本地运行的 Clash Verge Rev 增强配置生成工具。双击运行，自动完成「读订阅 → 测延迟 → 选中间节点 → 填出口 → 写回配置」全流程。

## 它能做什么

1. **自动检测当前订阅**：读 `profiles.yaml` 的 `current` 字段，任何机场/任何用户都能用，不写死。
2. **测延迟排序**：对所有节点做 TCP 端口握手测速，按延迟从低到高显示「编号 + 延迟 + 节点名」。
3. **选中间节点**：从列表挑一个作为住宅IP等出口的 `dialer-proxy` 中转跳板。
4. **填出口节点**：支持 URL、账号密码及 IPv6 格式，自动生成标准节点定义。
5. **写回配置**：自动写入当前订阅关联的 `proxies` 增强文件的 `prepend` 段（没有则新建），重启 Clash Verge 生效。

## 使用方法

1. 确保 `中间节点选择器.bat` 和 `中间节点选择器.ps1` 保持在同一文件夹，再双击 `.bat` 文件
2. 等待测速完成（约 20-60 秒），看到节点列表
3. 输入中间节点编号（回车可跳过）
4. 输入出口节点地址
5. 选择出口类型（socks5 / http）
6. 输入备注名（回车用默认）
7. 自动写回配置，重启 Clash Verge 生效

## 出口地址支持格式

| 输入示例 | 自动解析结果 |
|---|---|
| `socks5://user:pass@1.2.3.4:443` | 完整 URL，协议+认证+地址全识别 |
| `http://user:pass@1.2.3.4:8080` | http 类型 |
| `user:pass@1.2.3.4:443` | 无协议前缀，认证+地址 |
| `1.2.3.4:443` | 仅地址，无认证 |
| `1.2.3.4` | 仅 IP，端口默认 443 |
| `[2001:db8::1]:443` | IPv6 地址（必须使用方括号） |

## 生成结果示例

```yaml
prepend:
  - type: 'socks5'
    name: '示例住宅出口'
    server: '203.0.113.10'
    port: 443
    username: 'your-username'
    password: 'your-password'
    dialer-proxy: '你的中间节点名称'
append: []
delete: []
```

## 注意事项

- 工具仅访问本机 Clash Verge 配置，不上传订阅、节点名称或代理账号密码。
- 工具会自动备份原增强文件为 `.bak`，同时额外保留一份带时间戳的备份，误改可恢复。
- 占位假节点（`update.microsoft.com` / `1.1.1.1` 等）已自动过滤，不会出现在列表里。
- 测速是 TCP 握手延迟，是「能连通 + 大致快慢」的近似值，非真实代理下载速度。
- 备注、账号和密码中的单引号会自动转义为有效 YAML；输入内容不能包含换行。
- 纯 bat + PowerShell，零依赖，无需安装额外软件。
